services:
  api-gateway:
    ports:
      - 8080:8080
  ebikes:
    volumes:
      - ${HOME}/Desktop/EBikeApp/db:/data/db:rw
    ports:
      - 8081:8080
  users:
    volumes:
      - ${HOME}/Desktop/EBikeApp/db:/data/db:rw
    ports:
      - 8082:8080
