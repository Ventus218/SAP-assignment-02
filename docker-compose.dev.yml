services:
  ebikes:
    volumes:
      - ${HOME}/Desktop/EBikeApp/db:/data/db:rw
    ports:
      - 8081:8080
  users:
    volumes:
      - ${HOME}/Desktop/EBikeApp/db:/data/db:rw
    ports:
      - 8080:8080
